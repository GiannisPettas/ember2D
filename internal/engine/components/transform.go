package components

// Position represents an entity's location in 2D space.
type Position struct {
	X, Y float64
}

// Velocity represents an entity's speed and direction.
type Velocity struct {
	X, Y float64
}
